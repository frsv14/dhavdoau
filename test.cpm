class Node {

};

main(): int {
  volatile n : int
  read(n)

  for (volatile i : int := 2, i <= n, i := i + 1) {
      for (volatile i : int := 2, i <= n, i := i + 1) {
          for (volatile i : int := 2, i <= n, i := i + 1) {

          }
      }
  }

  volatile i : int := 2

  return 0
}